{
  "rules": {
    "usuarios": {
      "$uid": {
        ".read": "$uid === auth.uid",
        "esPremium": {
          ".write": false
        },
        "fechaPremium": {
          ".write": false
        },
        "premiumPurchaseTokenHash": {
          ".write": false
        },
        "$other": {
          ".write": "$uid === auth.uid && $other !== 'esPremium' && $other !== 'fechaPremium' && $other !== 'premiumPurchaseTokenHash'"
        }
      }
    },
    "premiumPurchaseTokens": {
      ".read": false,
      ".write": false
    },
    "_backups": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
